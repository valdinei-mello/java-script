/* alert('Olá Mundo !');

confirm("Deseja realmente apagar? ");

let nome = prompt("Digite seu nome: ");
alert(nome);

*/

// Inserir dois numeros e exibir a sua soma.

const n1 = Number(prompt("Digite 1° numero: "));
const n2 = Number(prompt("Digite 2° numero: "));

const resp = n1 + n2;
alert(`Seu resultadoé ${resp}`);

